
<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header">Categories Management</h1>
  </div>
  <div class="clear"></div>
  <% if flash[:warning] %>
      <div class="alert alert-warning">
        <strong>Warning!</strong> <%= flash[:warning] %>
      </div>
  <% end %>
  <% if flash[:success] %>
      <div class="alert alert-success">
        <strong>Success!</strong> <%= flash[:success] %>
      </div>
  <% end %>
  <% if notice %>
      <div class="alert alert-success">
        <strong>Success!</strong> <%= notice %>
      </div>
  <% end %>
  <!-- /.col-lg-12 -->
</div>
<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-heading">
        <%= button_to 'New',new_product_path,class: 'btn btn-success',style: '',method: :get %>
      </div>
      <!-- /.panel-heading -->
      <div class="panel-body">
        <div class="dataTable_wrapper">
          <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
            <thead>
            <tr>
              <th>Name</th>
              <th>Price</th>
              <th>Avatar</th>
              <th>Description</th>
              <th>Category</th>
              <th>Status</th>
              <th>Created At</th>
              <th>Setting</th>
            </tr>
            </thead>
            <tbody>
            <% @product.each do |p| %>
              <tr class="odd gradeX">
                <td><%= p.name %></td>
                <td><%= p.price %></td>
                <td><%= image_tag p.avatar.url(:thumb) %></td>
                <td><%= raw p.description %></td>
                <td><%= p.category.name %></td>
                <td><%= p.status %></td>
                <td><%= p.created_at %></td>
                <td>
                  <div class="row">
                    <div class="col-sm-3"><%= button_to 'Edit',edit_product_path(id: p.id),class: 'btn btn-warning',style: 'float:left;margin-right:10px',method: :get %></div>
                    <div class="col-sm-6"><%= button_to('Delete',p, {:onclick => "return confirm('Are you sure?')", :method => :delete, :remote => true,class: 'btn btn-danger'}) %></div>
                  </div>
                </td>
              </tr>
            <% end %>
            </tbody>
          </table>
        </div>
        <div class="text-center">
          <div class="pagination">
            <%= will_paginate @product, renderer: BootstrapPagination::Rails %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
