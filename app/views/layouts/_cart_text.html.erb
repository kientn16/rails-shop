<%= link_to "#{current_order.order_items.size} Items in Cart ( #{number_to_currency(current_order.subtotal, unit: "VND", separator: ",", delimiter: ".", format: "%n %u",strip_insignificant_zeros: true)} )", cart_path, class: "btn btn-link" %>
